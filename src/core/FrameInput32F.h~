#ifndef FrameInput32F_H_
#define FrameInput32F_H_

//Opencv
#include "FrameInput.h"

using namespace std;

class FrameInput32F: public FrameInput{
	public:
	FrameInput32F();
	FrameInput32F(Calibration * cal, pcl::PointCloud<pcl::PointXYZRGB> & input_cloud, int i, string path);
	FrameInput32F(Calibration * cal, string rgb_path, string depth_path);
	~FrameInput32F();
/*
	float *** full_getXYZ(int & w, int & h);
	void getXYZ(float & x, float & y, float & z,int w, int h);
	void vector_getXYZ(vector<float> & x, vector<float> & y, vector<float> & z,vector<int> & w, vector<int> & h);

	pcl::PointCloud<pcl::PointXYZRGB> getCloud();
*/
};
#endif
